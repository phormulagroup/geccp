# Carregado por "npm run build" (modo "production", o modo por defeito do Vite)
# Ainda não existe uma API de produção separada da staging — atualizar este
# valor assim que existir.
VITE_API_URL=https://geccpapi.phormuladev.com
