# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

./server/node_modules
./webapp/node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables / secrets
.env
.env.*
!.env.example
# Webapp env files hold no secrets (only public API URLs) — kept in git so
# builds are reproducible across machines/CI.
!webapp/.env.development
!webapp/.env.staging
!webapp/.env.production

# Deploy packages
deploy
